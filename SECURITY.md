# 隐私与安全

本项目面向个人本机使用，仅监听 127.0.0.1。Host/Origin 检查和会话令牌限制跨站访问，但不能抵御已控制电脑、浏览器或同一用户会话的程序。没有多用户身份认证，不适合直接开放公网。

## 数据流

| 数据 | 保存或发送位置 |
| --- | --- |
| DeepSeek、Tavily 密钥 | 当前 Node 进程内存；请求对应服务时发送；不写入备份 |
| 角色及历史 | 浏览器 localStorage，明文；导出文件也包含历史 |
| 真实聊天 | 当前角色和最近最多 20 条消息（历史最多 16000 字符）发送 DeepSeek |
| 模型知识生成 | 角色名字和作品信息发送 DeepSeek |
| 网页检索 | 角色查询发送 Wikipedia 或 Tavily；节选发送 DeepSeek |

第三方服务对数据的处理以其条款为准。本程序没有主动添加分析或遥测服务。关闭后台清除内存密钥；这不删除第三方已经收到的请求，也不删除浏览器历史。

## 提交前检查

运行 `npm run check:privacy`。检查器使用发布文件清单，拒绝额外文件，并扫描常见密钥格式、私钥头和个人绝对路径；它不是完整的安全审计，也无法识别任意形式的秘密。测试里的 fake/test 密钥都是虚构字符串。

不要提交 .env、日志、聊天导出、浏览器配置、个人截图或真实请求录制。`.gitignore` 不会清除已经提交的文件；若密钥曾进入 Git 历史，应在服务商处撤销，并清理历史后再发布。

报告问题时只附最小复现和脱敏错误信息，不上传密钥或完整对话。仓库尚未设置私人漏洞接收渠道；发布者应在 GitHub 仓库开启私人漏洞报告，避免把可利用细节直接公开。
