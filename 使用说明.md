# 百变虚拟角色

## 快速开始：根据模型已有知识生成
1. 双击 start.cmd，保持后台窗口打开，在浏览器访问 http://127.0.0.1:3210 。
2. 右上角“连接设置”填写 DeepSeek API 密钥。密钥只存在后台内存中，不进入浏览器存储或备份；后台重启后需重新填写。
3. 在左侧输入角色名，例如“樱岛麻衣”；作品可选填，名字有歧义时建议补充。
4. 保持默认的“模型已有知识 · 未联网核实”，点击“生成分层角色并切换”。
5. DeepSeek 生成角色后会自动保存并切换。到“角色资料库”查看设定，在“编辑角色”中调整互动方式；修改核心需创建自定义分支。

这个模式只向 DeepSeek 发送请求，不访问百科或搜索服务，不需要 Tavily 密钥。它仍需要网络和 DeepSeek API 余额，不是离线模型。

## 生成结果代表什么
内容来自模型已有知识，没有联网核实。模型可能记错设定、混淆版本，或无法识别冷门角色。界面会明确标注“未联网核实”，不会伪造来源网址或把模型记忆列为已验证事实。

资料库包括身份、表达风格、偏好、边界、未核实背景、原创示例及不确定之处。这是用角色资料指导模型对话，没有训练或修改模型参数。同名角色可能显示候选供确认；不能识别或调用失败时不会覆盖原角色。

## 其他模式
- 自定义：点击“创建自定义角色”，直接填写人设，不消耗生成调用；真实聊天仍会调用 DeepSeek。
- 百科检索：保留原有功能，无需搜索密钥，但需能连接维基百科。本次之前的网络测试出现连接超时。
- 全网检索：需在连接设置中另外填写 Tavily 搜索密钥。Tavily 搜索与 DeepSeek 分析分别按账户额度计费。

联网失败不会自动切换到模型知识模式，以免混淆资料来源。需要时可以手动选择新的生成方式。

## 保存与兼容
角色和对话保存在原来的浏览器存储位置，应用改名不改变存储键，也不会改名或删除已有角色。旧角色“微光”可继续作为一个独立角色存在。新安装的默认示例名为“初始角色”。

每个角色有独立记录。最多保存 30 个角色，每个角色最多 2000 条消息。导出备份名称改为“百变虚拟角色-日期.json”，仍支持以前的单角色及角色库备份。导入会确认并替换现有库，导出不含密钥。

普通编辑只修改互动层。若要修改身份或核心，在资料库选择“创建自定义分支 / 审校核心”；保存成独立角色，原角色与记录保留。重复生成相同名字、作品和方式时，会优先使用已有深层资料的未编辑缓存。在资料库点击重新生成可产生新档案，原档案不受影响。

## 分层角色资料库
① 不可修改设定：姓名、作品、身份背景，保存后在普通编辑器中只读。

② 核心设定：性格、偏好、边界、价值排序、动机、内在矛盾、情绪变化、人际差异、说话节奏、幽默与容易失真的写法。普通编辑只读；纠错或改编请创建自定义分支。新生成档案还会请求 4～6 个不同情境的原创回应，帮助模型理解行动原因，而不是每轮套用台词。

③ 可修改设定：与你的关系、称呼、虚构场景、回复篇幅、主动程度。例如关系可以写“我扮演梓川咲太，按原作中与麻衣的关系互动”，场景中补充作品时期。互动调整不能覆盖角色的身份和核心人格。

这是应用编辑器与对话提示的分层约束，不是防篡改系统，也不能保证模型绝不偏离。只读不代表资料正确：模型生成的核心也需要审校。旧档案会兼容转换，但不会自动补齐新内容；请重新生成来获得更丰富的人格资料。

全网模式会分别检索人物资料和台词/口癖，每次通常执行两次 Tavily 搜索，再交给 DeepSeek 分析；不是打开本地浏览器，也不是 DeepSeek 接口天然自带联网。百科模式仍只检索百科，可能找不到对白。检索不到时不会编造已查证台词。

资料库区分：能在网页节选中匹配的短引文、未核实的候选口癖、原创情境示例。网页匹配不能证明说话者、译文和语境完全正确，应查看来源。模型知识模式没有原作引文证据。当前没有抓取完整剧集，也没有训练模型参数。

## 费用
默认 deepseek-flash，一次生成通常调用一次 DeepSeek。相同档案命中缓存不重新调用。角色生成最多输出 5500 token，聊天最多 700 token，每次启动最多 100 次模型请求。较丰富的档案也会增加每次聊天的输入用量。候选确认、重新生成或手动重试可能增加调用。这不是金额上限，实际费用请查看服务商控制台。

聊天会发送人设、背景、风格与原创示例，以及最近最多 20 条真实消息（聊天历史最多 16000 字符）。不会携带其他角色的对话。模型知识模式没有额外搜索服务费用。

DeepSeek：https://platform.deepseek.com
价格：https://api-docs.deepseek.com/quick_start/pricing/

## 启动与排错
如果网页提示旧后台仍运行，请关闭旧启动窗口，重新运行 start.cmd，再刷新页面。页面过期也请刷新并重新填写密钥。密钥无效或余额不足时，按页面提示检查 DeepSeek 开放平台。

若端口 3210 已有本应用在运行，直接打开本地地址即可。保持使用同一浏览器和同一网址，定期备份；清理浏览器数据可能丢失记录。

## 验证
模型知识分支已经过模拟接口测试：仅访问 DeepSeek、无搜索调用、不保留模型伪造的来源、同名/未知/无密钥/错误返回、聊天正确使用角色背景；原有检索测试也已通过。没有使用真实密钥消耗账户额度来测试本次生成。
