# 参与开发

使用 Node.js 22+。无需安装第三方依赖；运行 `npm start` 开发，修改后刷新页面；服务端代码修改需要重启后台。

提交前运行 `npm test` 和 `npm run check:privacy`。模型与检索测试应注入模拟 fetch，避免消耗贡献者账户额度。新增发布文件需同步 `scripts/release-files.json`。

请在 PR 中说明问题、变化和验证方式。界面文案保持中文易懂；演示状态、资料来源和 API 费用不可隐藏。不要上传用户聊天、密钥、完整原作台词或未经授权素材。

角色层级改变时应验证旧档案兼容性。不要随意更换 localStorage 键，否则用户会以为记录丢失。提交代码按仓库 MIT 许可证提供。
